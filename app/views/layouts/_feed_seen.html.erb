<% if @seen_posts.any? %> 
  <ol class="post">
    <%= render @seen_posts %> 
  </ol>
  <%= will_paginate @seen_posts, params: { controller: :pages, action: :feed } %>
<% else %>
  <h1>You have no posts seen</h1>
<% end %>